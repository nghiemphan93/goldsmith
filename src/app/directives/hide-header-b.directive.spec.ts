import { HideHeaderBDirective } from './hide-header-b.directive';

describe('HideHeaderBDirective', () => {
  it('should create an instance', () => {
    const directive = new HideHeaderBDirective();
    expect(directive).toBeTruthy();
  });
});
